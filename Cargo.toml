[package]
name = "iris"
version = "0.1.0"
edition = "2021"
description = "Syntax highlighter for git diff and git log -p output"
license = "MIT OR Apache-2.0"
repository = "https://github.com/mizzy/iris"

[dependencies]
libc = "0.2"
similar = "2"
strip-ansi-escapes = "0.2"
syntect = "5"
